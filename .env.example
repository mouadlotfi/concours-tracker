# ── Required ───────────────────────────────────────
APP_BASE_URL=http://localhost:3000

# ── Brevo (email service) ─────────────────────────
BREVO_API_KEY=
BREVO_LIST_ID=
BREVO_SENDER_EMAIL=
BREVO_SENDER_NAME=Concours Développement Web

# ── Security (required for production) ────────────
CRON_SECRET=
UNSUBSCRIBE_SECRET=

# ── Scheduler ─────────────────────────────────────
# Cron expression for automatic refresh (default: every 5 hours)
# REFRESH_CRON=0 */5 * * *

# ── Data persistence ──────────────────────────────
# Directory for the local JSON store (default: ./data)
# DATA_DIR=./data
